[tool.poetry]
name = "repl_python3_Divergence"
version = "0.1.0"
description = ""
authors = ["samthepacman <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
tqdm = "^4.64.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
